const initialState = {
  averages: [],
  sisbens: [],
  population: [],
  inscriptions: [],
};

export const inscriptionReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
